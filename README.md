# Polyglot Concordance — Viewer

> ⚠️ **This is a pilot.** Current scope is the Gospel of Mark only. The full plan — additional NT books, the Hebrew Bible flagship, further witnesses, export formats, and two planned sub-projects — lives in [**ROADMAP.md**](./ROADMAP.md).

A word-by-word alignment of the **Gospel of Mark** across three textual witnesses — **Greek NT**, **Syriac Peshitta**, and **Latin Clementine Vulgate** — with a one- or two-sentence apparatus note on every divergence.

The apparatus is generated by Anthropic Claude, pre-computed as static JSON, and served by a small Flask app. No runtime LLM calls.

> 🔗 **Live:** [polyglotconcordance.com](https://polyglotconcordance.com) · run locally at [http://localhost:5020](http://localhost:5020) (see [Quickstart](#quickstart)).

---

## What it does

- **676 Mark verses aligned word-by-word** across three traditions, every verse classified, every divergence typed and annotated.
- **Three view modes per verse**:
  - **Interlinear** — stacked rows, one group of aligned tokens per slot
  - **Alignment grid** — Chen-style parallel columns, one row per alignment group
  - **Critical apparatus** — scholarly cards with sigla chips, variant type, and the prose note
- **Color-coded variants** — gold (aligned), sage green (minor / stylistic), rubric red (major / omitted / added)
- **Click-to-open tooltips**:
  - Greek tokens → Strong's number + lemma + morphology (STEP Bible TAGNT)
  - Peshitta tokens → triliteral root + sister roots + Hebrew / Arabic cognates ([Aramaic Root Atlas](https://aramaic-root-atlas.onrender.com))
- **Shift-click a variant token** → inline apparatus popover without leaving the view
- **⌘K / Ctrl-K search** across Greek, Latin, Syriac, English (WEB), variant type, or by reference (`13:14`)
- **Full keyboard navigation** (`←` `→` paginate verses, `⌘K` search, `Esc` close overlays)

## Methodology

Every verse is aligned by Anthropic's **Claude Sonnet 4.5** via the Messages Batch API. For each verse, Claude receives:

- The Greek, Syriac, and Latin tokens (whitespace-split)
- Per-Greek-token enrichment: Strong's number, lemma, morphology, English gloss (from STEP TAGNT)
- Per-Peshitta-token root data (from the Aramaic Root Atlas)

Claude returns a strict JSON structure: a list of alignment groups where each group ties one or more tokens from each tradition together and tags:

- **Verdict** — `aligned`, `minor`, `major`, `omitted`, `added`
- **Type** — `agreement`, `expansion`, `omission`, `substitution`, `harmonisation`, `word-order`, `construction`, `idiom`, `punctuation`, `grammar`, `lexical`, `gloss`
- **Note** — one or two sentences of apparatus-style prose for every non-aligned group (mean length ~296 chars)
- **Confidence** — Claude's self-reported confidence `[0, 1]`

All 676 verses × 8,206 alignment groups are stored as committed JSON in `data/alignments/mark/`. The running viewer has zero runtime LLM dependency.

### Methodology validation (and its limits)

We sanity-checked Claude's alignment reasoning against the **[Berean Interlinear Bible](https://berean.bible/)** on the narrow sub-task of Greek → English word alignment. Across all 673 Mark verses (6,175 scoring tokens), Claude agreed with Berean's scholarly glosses on **67.7%** of tokens.

The ~32% of non-matches are mostly **translation-word-choice differences** between Berean and the WEB (the two English translations involved in the comparison) — e.g. Berean "crowd" vs. WEB "people" for the same Greek ὄχλος — rather than alignment errors.

**What this benchmark means:**

- ✅ It's a **transferable sanity check**: Claude's alignment reasoning is consistent with an established scholarly interlinear on the sub-task we *can* measure (Greek→English). That rules out catastrophic failure modes and gives some evidence the broader 3-way alignments use the same reasonable logic.
- ❌ It does **not** directly measure the quality of Greek↔Peshitta, Greek↔Vulgate, or Peshitta↔Vulgate alignments — no ground-truth reference exists for those pairs. It also doesn't measure apparatus-note quality, variant classification accuracy, or type-tag correctness.

Reproduce it yourself: `python scripts/run_berean_benchmark.py --berean data/benchmarks/berean_mark_source.json --sample-size 673`

## Data sources

| Layer | Source | License |
|---|---|---|
| Greek NT (tagged) | [STEP Bible TAGNT](https://github.com/STEPBible/STEPBible-Data) (Tyndale House Cambridge) | CC BY 4.0 |
| Peshitta NT (text) | [Aramaic Root Atlas](https://aramaic-root-atlas.onrender.com) | derived from public Peshitta sources |
| Peshitta roots + cognates | [Aramaic Root Atlas](https://aramaic-root-atlas.onrender.com) | see project |
| Clementine Vulgate | [seven1m/open-bibles](https://github.com/seven1m/open-bibles) (USFX) | public domain |
| English verse gloss | World English Bible (WEB) | public domain |
| Alignment generation | Anthropic Claude Sonnet 4.5 | — |
| Benchmark reference | [Berean Interlinear Bible](https://berean.bible/) | methodology check only |

## Tech stack

- **Backend**: Python 3.10+, Flask 3, Jinja2 — single `app.py`, thin route layer; business logic in `translation_core/`
- **Frontend**: Server-rendered HTML + vanilla JavaScript + one CSS file. No build step, no framework.
- **Interactivity**: htmx-friendly partial endpoints for per-verse swaps; `<script id="variants-data" type="application/json">` for the variant popover
- **Typography**: EB Garamond + Cardo (scripture body, Greek + Latin), Noto Sans Syriac (Peshitta), Inter (UI chrome), JetBrains Mono (sigla / captions)
- **Data**: CSV corpora + per-verse JSON alignments, all committed. Total data footprint ~3 MB.
- **Tests**: 70+ pytest cases covering schema, converter, routes, search, benchmark. `pytest -q` runs the full suite in < 1 second.

## Quickstart

```bash
# 1. Clone
git clone https://github.com/Jossifresben/translation-alignment.git
cd translation-alignment

# 2. Python env + deps
python -m venv .venv
source .venv/bin/activate
pip install -r requirements-dev.txt

# 3. (Optional) Add your Anthropic API key — only needed if you want to
#    regenerate alignments or run the Berean benchmark yourself
cp .env.example .env
# edit .env and paste your key

# 4. Run tests
pytest

# 5. Run the viewer
python app.py
# → http://localhost:5020
```

No network calls at runtime. The API key is referenced only by offline scripts in `scripts/`.

## Regenerating alignments

Everything is reproducible from source data + an API key:

```bash
# 10-verse pilot (sanity check — ~$0.05, ~2 min)
python scripts/generate_alignments.py --pilot

# Full Mark, Batch API (~$1.50, ~20 min wall clock, 50% batch discount)
python scripts/generate_alignments.py --full

# Regenerate a single verse
python scripts/generate_alignments.py --verse Mark 13 14

# Flag low-confidence verses for human review
python scripts/generate_alignments.py --report

# Re-run the Berean benchmark
python scripts/run_berean_benchmark.py \
  --berean data/benchmarks/berean_mark_source.json \
  --sample-size 673
```

## Project layout

```
app.py                          Flask app — thin route layer
translation_core/               Python package
  corpora.py                    CSV loading + verse lookup
  alignment.py                  Per-verse JSON loader + cache
  schema.py                     Alignment JSON validator
  enrichment.py                 Strong's + Peshitta root lookup
  converter.py                  Our JSON → designer witness/variants shape
  rendering.py                  Alignment → HTML (tokens with variant classes)
templates/                      Jinja: home, verse, _interlinear, _alignment_grid,
                                _apparatus, _rail, about, macros
static/css/styles.css           One stylesheet, CSS variables, parchment palette
static/js/aligner.js            Vanilla JS: hover-link, popover, search, tooltips
data/
  corpora/                      CSV: greek_nt, peshitta_nt, vulgate, web
  alignments/mark/<ch>/<v>.json The 676 per-verse alignment files
  enrichment/                   STEP Strong's + Peshitta roots
  benchmarks/                   Berean source + preflight result
  pericopes.json                Pericope labels for Mark
scripts/                        Ingestion + generation + benchmark scripts
tests/                          pytest suite
docs/superpowers/               Design spec + implementation plan
ROADMAP.md                      What's next
known-issues.md                 Data drift + caveats worth knowing
```

## What this app is good for

The underlying engine is a **multi-witness parallel-text viewer with a critical apparatus**. Anywhere a text exists in more than one version, this tool can show it side-by-side with per-word alignment and annotated divergences.

### Biblical scholarship

- **Text-critical study** — see where a Greek NT verse, its Peshitta translation, and its Vulgate rendering disagree, with each divergence typed (harmonisation, substitution, idiom, grammar change, etc.) and explained in one or two sentences.
- **Synoptic comparison** — once Matthew / Mark / Luke all ship, compare the same pericope across the three Synoptic gospels in three languages each.
- **Semitic-language parallel reading** — Peshitta next to Hebrew OT next to Targum Onkelos; root / sister-root tooltips surface cognate structure visibly.
- **Translation-technique study** — how does the Peshitta handle Greek participles? How does the Vulgate handle Hebrew infinitive absolutes? The typed apparatus makes this kind of question quickly answerable.
- **Harmonisation tracing** — when one tradition imports material from a parallel passage (e.g. Peshitta Mark 13:14 pulling "by Daniel the prophet" from Matt 24:15), the apparatus flags it explicitly.

### Teaching

- **Ancient-language pedagogy** — introductory Greek / Hebrew / Syriac / Latin students can hover any word for Strong's / lemma / morphology. The variant apparatus turns every verse into a textual-criticism lesson.
- **Comparative religion** — a neutral surface for showing how different canonical lineages transmit the same base text.
- **Critical-edition training** — readers learn to read a scholarly apparatus by clicking through real examples rather than deciphering the dense sigla of a printed NA28 / BHS.

### Translation work

- **Checking a new translation against ancient witnesses** — does a contemporary Spanish or Chinese rendering match the majority of ancient traditions or diverge meaningfully?
- **Informing committee decisions** — Bible translation teams can point to a specific divergence pattern (e.g. "MT + LXX + Peshitta all read X; only Vulgate reads Y") in one clickable reference.

### Reader-facing discovery

- **Scripture reading with a critical dimension** — readers can see that a familiar phrase has a contested history without opening a textbook.
- **Liturgical comparison** — Eastern Orthodox liturgies use Slavonic + Greek Byzantine; Catholic uses Vulgate; churches of the East use Peshitta. All three surface-able side by side.

## Roadmap

### Recently shipped

- **Internationalization (i18n)** — localized URLs (`/es/`, `/zh-Hans/`, `/zh-Hant/`); first-visit auto-redirect from `Accept-Language`; sticky `lang` cookie; full Spanish UI with RV 1909 verse glosses; multi-language gloss stack above the alignment grid; hreflang SEO; localized sitemap

### Up next

- **Syriac font selection** — Noto Sans Syriac (current default) / Estrangelo Edessa / Serto (Jacobite) / East Syriac (Nestorian) / Madnhaya, with glyph preview and per-user persistence
- **Settings panel in the top bar** — consolidates theme, language, Syriac font, and rail visibility behind a single cog icon

### Expand book coverage

**New Testament** (continuing the current scope):

- **All four gospels** — Matthew / Luke / John (pipeline already supports this)
- **Acts** — Greek NT historical narrative; Vulgate and Peshitta already cover it
- **Pauline epistles** — Romans through Philemon
- **General epistles and Revelation** — James, 1–2 Peter, 1–3 John, Jude, Revelation

**Hebrew Bible / Tanakh** (a separate flagship):

- **Torah (Pentateuch)** — the natural starting point. Genesis, Exodus, Leviticus, Numbers, Deuteronomy aligned across:
  - Masoretic (Leningrad / Aleppo) + LXX (Rahlfs or Swete) + Peshitta OT + Vulgate OT
  - **Samaritan Pentateuch** (~6,000 meaningful variants from the MT — this viewer's apparatus would be a useful study surface)
  - **Targum Onkelos** + **Targum Pseudo-Jonathan**
- **Former Prophets** — Joshua, Judges, Samuel, Kings
- **Latter Prophets** — **Isaiah** (Dr. Chen's own dissertation focus, rich Peshitta scholarship available), Jeremiah, Ezekiel, the Twelve Minor Prophets
- **Dead Sea Scrolls witnesses** where extant — 1QIsaᵃ, 4QSamᵇ, etc.
- **Writings (Ketuvim)** — Psalms, Proverbs, Job, Megillot (Song of Songs, Ruth, Lamentations, Ecclesiastes, Esther), Daniel, Ezra-Nehemiah, Chronicles

**Deuterocanonical / Apocrypha:**

- Wisdom of Solomon, Sirach, Tobit, Judith, Maccabees, Baruch, etc. — relevant to Catholic, Orthodox, and Ethiopian canons

### Expand witness coverage (within current books)

- **English (WEB)** as a first-class 4th column, with alignments regenerated across four traditions
- **Greek Byzantine** (Majority Text / TR) — diffs against NA28 surface as a fifth witness column
- **Orthodox Chinese cluster** — Slavonic 1751 (Elizabeth Bible) + 1864 Küri (固里) + 1910 Innokenti (英诺肯提乙). Together: Russian-Orthodox Classical Chinese mission translations aligned against Church Slavonic + Greek
- **Coptic** (Sahidic + Bohairic), **Armenian** (Zohrab), **Ethiopic** (Ge'ez)
- **Latin Vetus / Old Latin** — pre-Jerome Latin witnesses
- **Targum Onkelos + Biblical Aramaic + Peshitta OT** — already in the Aramaic Root Atlas; wire up when Hebrew OT scope lands

### Viewer features

- Accessibility audit — full aria pass, keyboard-only navigation, high-contrast / dark mode polish
- Cross-references in variant notes (e.g. "cf. Matt 24:15") rendered as clickable jumps between parallel verses
- Per-chapter navigation panel with verse counts + low-confidence flags + manual-review markers
- Bookmarks / permalinks — deep-link to a specific variant stays stable, shareable "copy link" affordance
- Mobile polish — one-column responsive layout, larger tap targets, swipe between verses
- Peshitta root-card on hover — hover any Peshitta token to see triliteral root + gloss + sister roots + Hebrew / Arabic cognates (from the Aramaic Root Atlas); click still opens the full Strong's-style tooltip

### Export

- **TEI XML** export of the critical apparatus per TEI P5 `<app>`/`<rdg>` conventions
- **BibTeX** citation export per verse / per variant
- **CSV** bulk export of all alignments for downstream analysis
- Public read-only JSON API at `/api/v1/` — alignment, verse, search, manifest endpoints with OpenAPI docs at `/api/docs`. CC BY 4.0 (derived data); CORS-open. Try [/api/v1/manifest](https://polyglotconcordance.com/api/v1/manifest)

### Sub-projects

- **Sub-project 2 — Machine Annotation Engine**: live, on-demand alignment for verses the pilot doesn't cover. REST endpoint `/api/align` with per-request confidence score, caching, rate-limiting, optional custom-text override, and a prompt-engineering dashboard.
- **Sub-project 3 — Scholar Review Workflow**: accounts + auth (ORCID / GitHub / Google), student / scholar / editor roles, review queue pulling from the engine and flagged low-confidence verses, threaded per-verse comments with alternative-alignment proposals, consensus-threshold publication as a **gold standard**, TEI / BibTeX / CSV export with Zenodo DOI per corpus release, per-alignment revision history and contributor attribution.

Full list with context in [**ROADMAP.md**](./ROADMAP.md).

## Known limits

See [`known-issues.md`](./known-issues.md) for the complete list. Highlights:

- Versification: Vulgate chapter-verse numbering differs from NA28 in some places. We've remapped Mark 9 and noted Mark 4:40–41; other chapters and books will need the same treatment as coverage expands.
- 29 Mark verses have alignment confidence below 0.30 (mostly short or compositionally unusual verses). They're flagged for human review via `scripts/generate_alignments.py --report`.
- No ground-truth reference exists for the Peshitta or Vulgate alignment quality — only the Greek→English sub-task is externally benchmarked.

## Author

**Jossi Fresco Benaim** · ORCID [0009-0000-2026-0836](https://orcid.org/0009-0000-2026-0836)

## Related projects

- [Aramaic Root Atlas](https://aramaic-root-atlas.onrender.com) — cross-corpus Semitic root explorer (used for Peshitta roots + cognates)
- [Peshitta Constellations](https://peshitta.onrender.com/) — companion project exploring the Peshitta corpus
- [BibCrit](https://bibcrit.app/) — biblical criticism workspace for textual analysis

## Inspiration

The parallel-columns layout and the minor / major color-coding convention come from the **bible-mt5** parallel viewer built by [Dr. Zhan Chen](https://staff.uic.edu.cn/zhanchen/en) (United International College / BNU-HKBU UIC, Zhuhai). Dr. Chen's own scholarship focuses on the Peshitta of Isaiah (Harvard NELC, 2020) and Chinese Bible translations.

## License

The viewer code is **intended to be released under an open-source license** once the project is publicly funded; until then, the repository is private and all rights are reserved.

The derived alignment JSON is produced from sources with mixed licenses (CC BY 4.0 for STEP-derived data; public domain for Vulgate, WEB). Any future redistribution will credit the upstream sources.
